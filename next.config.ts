import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["media.giphy.com", "i.postimg.cc", "media0.giphy.com", "media1.giphy.com", "media2.giphy.com"],
  },
};

export default nextConfig;
