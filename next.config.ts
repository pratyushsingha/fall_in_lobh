import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "media.giphy.com",
      "i.postimg.cc",
      "media3.giphy.com",
      "media0.giphy.com",
      "ucarecdn.com",
      "media2.giphy.com",
    ],
  },
};

export default nextConfig;
